namespace Assets.Scripts.BehaviourTree
{
    /// <summary>
    /// Used to create specific behaviour trees.
    /// </summary>
    public enum Difficulty
    {
        Plein,
        Door,
        Easy,
        Medium,
        Hard
    }
}